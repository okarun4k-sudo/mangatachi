name: Notificar Discord Automático
on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Permite salvar o arquivo de cache no seu repositório
    steps:
      - name: Checkout do código
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 

      - name: Configurar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Rodar Script
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: node notificar.cjs # Garante a execução do script de alerta

      - name: Salvar Alterações no Cache
        run: |
          git config --global user.name "Mangatachi Bot"
          git config --global user.email "bot@mangatachi.com"
          git add manga_cache.json
          # O [skip ci] evita que o bot entre em loop infinito ao salvar o arquivo
          git commit -m "chore: update manga cache [skip ci]" || echo "Nada para atualizar"
          git pull --rebase origin main
          git push origin main
